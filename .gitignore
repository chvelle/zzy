# secrets: never
.env
.env.*
!.env.example
site/.env.local
site/.env.vercel
site/.vercel
.vercel

# dependencies
node_modules/

# state the bot writes while it runs: yours, not the repo's
data/positions.json
data/positions.json.bak
data/paper-positions.json
data/fork-positions.json
data/paper-ledger.json
data/fork-ledger.json
data/prices.json
data/notebook.json
data/notebook.json.bak
data/pnl-history.json
data/earnings.json
data/social-log.json
data/*.tmp
treasury/
decisions/*.json
!decisions/.gitkeep
site/data.json
site/history.json
site/feed.json

# the catalog is regenerated by npm run catalog:refresh, but a copy is
# useful for a fresh clone, so it is tracked. Everything above is not.
